<%
@songs.each do |song| %>
	Title <%= song["songtitle"] %>
	Artist <%= song["name"] %>
	Album <%= song["albumtitle"] %>
	Genre <%= song["genre"] %>
	Rating <%= song["rating"] %>
	Length <%= song["length"] %>
	<br>
<% end %>

